/**
 * An empty component used for wrapping other elements. This useful when React expects a single
 * child to be provided to another component.
 */
const Wrapper = ({ children }) => children;
export default Wrapper;
